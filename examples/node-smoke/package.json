{
  "name": "@feelc-examples/node-smoke",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Parity + cross-tool smoke test: @feelc/engine (WASM) vs the native feelc CLI.",
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@feelc/engine": "*"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}

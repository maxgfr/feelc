{
  "branches": ["main"],
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    [
      "@semantic-release/exec",
      {
        "publishCmd": "if ! git rev-parse -q --verify refs/tags/v${nextRelease.version} >/dev/null; then git tag v${nextRelease.version} && git push origin v${nextRelease.version}; fi && goreleaser release --clean"
      }
    ]
  ]
}
